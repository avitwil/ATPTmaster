{
  "id": "agent_offensive",
  "name": "LLM Offensive Agent",
  "phase": "recon",
  "entrypoint": "module:AgentOffensive",
  "consumes": [],
  "provides": ["asset"],
  "intrusive": false,
  "run_modes": ["step", "semi", "full"],
  "enabled": true,
  "extracted_from": "atpt-native",
  "description": "LLM-driven recon->exploit ReAct loop, opt-in via config.offensive_agent.enabled. No-op when disabled (so it never gates a normal run). When enabled its human checkpoint is the startup scope-confirmation gate, and EVERY command passes the deterministic ScopeGuard (scope wall + allowlist + denylist) in every mode."
}
